# 2LIMS 部署包文件说明

## 部署包结构

```
2LIMS-deploy-package/
├── 📁 docker/                          # Docker 部署方式（推荐）
│   ├── docker-compose.yml
│   ├── nginx.conf
│   ├── start-docker.bat                # 一键启动
│   ├── stop-docker.bat                 # 停止服务
│   ├── logs-docker.bat                  # 查看日志
│   └── DOCKER_DEPLOYMENT.md            # Docker 部署文档
│
├── 📁 windows/                         # Windows 原生部署方式
│   ├── nginx.conf
│   ├── start-lims.bat                   # 启动脚本
│   ├── stop-lims.bat                    # 停止脚本
│   ├── install-as-service.bat           # 注册为 Windows 服务
│   ├── application.yml.template         # 配置模板
│   ├── WINDOWS_DEPLOYMENT.md            # Windows 部署文档
│   └── TROUBLESHOOTING.md               # 故障排查手册
│
├── 📁 server/                           # 后端程序
│   └── lims-server-1.0.0.jar             # 后端 jar 包
│
├── 📁 client/                           # 前端文件
│   └── dist/                            # 前端构建产物
│       ├── index.html
│       └── assets/
│
└── 📁 database/                         # 数据库脚本
    └── migration/                       # SQL 初始化脚本
        ├── create_biz_consultation_table.sql
        ├── create_biz_quotation_table.sql
        └── ...
```

---

## 部署方式对比

| 特性 | Docker 部署 | Windows 原生部署 |
|------|-------------|-----------------|
| **部署难度** | ⭐ 简单 (5分钟) | ⭐⭐⭐ 复杂 (30分钟+) |
| **前置要求** | Docker Desktop | JDK + MySQL + Nginx |
| **环境隔离** | ✅ 完全隔离 | ❌ 可能冲突 |
| **数据迁移** | ✅ 简单 | ❌ 复杂 |
| **资源占用** | ⭐⭐ 较少 | ⭐⭐⭐ 较多 |
| **推荐场景** | ✅ 生产环境 | ⚠️ 仅用于无 Docker 环境 |

---

## 快速开始

### Docker 部署（推荐）

```cmd
# 1. 安装 Docker Desktop（首次需要）

# 2. 解压部署包到 C:\lims-docker\

# 3. 双击运行 start-docker.bat

# 4. 等待启动完成，自动打开浏览器
```

### Windows 原生部署

```cmd
# 1. 安装 JDK 17
# 2. 安装 MySQL 8.0
# 3. 解压 Nginx

# 4. 按照 WINDOWS_DEPLOYMENT.md 文档操作
```

---

## 给客户的部署文件清单

根据客户的网络环境和IT能力，选择合适的部署方式：

### 场景1: 有互联网、IT能力一般 → Docker 部署

提供以下文件：
- [x] `docker/` 目录（完整内容）
- [x] `server/lims-server-1.0.0.jar`
- [x] `client/dist/` 目录
- [x] `start-docker.bat` 快捷方式
- [x] `DOCKER_DEPLOYMENT.md` 文档

### 场景2: 无互联网、IT能力较弱 → Windows 部署

提供以下文件：
- [x] `windows/` 目录（完整内容）
- [x] `server/lims-server-1.0.0.jar`
- [x] `client/dist/` 目录
- [x] JDK 17 安装包（离线）
- [x] MySQL 8.0 安装包（离线）
- [x] Nginx for Windows（离线）
- [x] `WINDOWS_DEPLOYMENT.md` 文档
- [x] `TROUBLESHOOTING.md` 故障排查手册

---

## 部署后验证

部署完成后，按以下步骤验证：

### 1. 检查服务状态

```cmd
# Docker 方式
docker-compose ps

# Windows 方式
tasklist | findstr "java nginx"
```

### 2. 访问系统

- 浏览器打开: `http://localhost`
- 或: `http://[服务器IP]`

### 3. 登录测试

- 用户名: `admin`
- 密码: `admin123`

### 4. 功能测试

- [ ] 新建咨询
- [ ] 生成报价单
- [ ] 上传文件
- [ ] 下载报告

---

## 技术支持

如遇问题，请提供以下信息：

1. 部署方式（Docker / Windows 原生）
2. 错误截图
3. 日志文件
4. `docker-compose ps` 或 `tasklist` 输出

---

**版本**: 1.0
**更新时间**: 2026-01-04
